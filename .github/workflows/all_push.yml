name: all push in test branch

on: 
  push:
  workflow_dispatch:
    inputs:
      only-test:
        description: is it showing in main branch?

jobs:
  echo:
    name: echo
    runs-on: ubuntu-latest
    steps: [{"run": "echo all_push"}, {"uses": "actions/checkout@v4"}, {"run": "tree"}, {"run": "echo extrastep"}]
